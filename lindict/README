#
#
#
old-original:
这是使用opencc组件前的词库。使用该词库，在候选框中会同时显示平假名和片假名

new-withopencc:
这是使用opencc组件后的词库。使用该词库，在候选框中只会单独显示平假名或单独显示片假名。原理是只使用平假名词库，如果要输入片假名，就通过opencc组件将平假名转换为片假名。
更改如下：
删除了片假名的码表。

2020/03/08：
注：此目录中的词库已不再更新，只留作备份，最新词库以主目录中的kappajp86.dict.yaml为准。
